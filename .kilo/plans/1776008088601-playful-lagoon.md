# Implement Quantity Field in Payment Model's createBillingRecord Method

## Goal
Fix the incomplete implementation on line 293 of `app/Models/Payment.php` where `$this->quantity` is referenced but not defined, and replace it with the appropriate quantity from the related prescription.

## Analysis
- The Payment model does not have a `quantity` field in its fillable attributes
- When creating billing records for prescription payments, the quantity should reflect the dispensed quantity from the prescription
- The Prescription model has `quantity_dispensed` which represents the actual quantity that was dispensed and paid for

## Implementation Steps
1. Change line 293 in `app/Models/Payment.php` from:
   ```php
   'quantity' => $this->quantity, // Can you help me implement this
   ```
   to:
   ```php
   'quantity' => $this->prescription->quantity_dispensed,
   ```

## Verification
- Ensure the change correctly accesses the dispensed quantity from the prescription
- Verify that billing records are created properly with the correct quantity information
- Test that the billing functionality works for prescription payments

## Notes
- This change assumes that the payment amount (`$this->amount`) corresponds to the total price of the dispensed prescription quantity
- No additional fields need to be added to the Payment model as the quantity comes from the related prescription